import turtle
tina = turtle.Turtle()
tina.shape('turtle')

for i in range(0, 3):
    # this indented code repeats 3 times
    tina.left(120)
    tina.forward(200)




















# # This code is not indented, so it's
# # "outside" the loop, and does not repeat
# tina.penup()
# tina.forward(100)
# tina.pendown()
# tina.circle(20)
# # Try indenting it to see what happens!

turtle.done()

